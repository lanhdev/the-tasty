<!DOCTYPE html>
<html>
<head>
  <title></title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>
<body>
<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-sm-8">
    <div class="card">
      <img class="card-img-top" src=<%= @food_item.image_url_or_default %> alt=<%= @food_item.name %> height="400">
      <div class="card-block">
        <h4 class="card-title"><%= @food_item.name %></h4>
        <i class="fa fa-eye" style="display:inline-block;width: 20px;"></i><span style="display:inline-block;min-width: 30px;"><%= @food_item.hit_count %></span>
        <i class="fa fa-comments" style="display:inline-block;width: 20px;"></i><span style="display:inline-block;min-width: 30px;"><%= @food_item.reviews.count %></span>
        <div class="star-rating" data-score= <%= @avg_rating %> ></div>
        <p><%= @food_item.description %></p>
        <%= link_to "#{number_to_currency(@food_item.price, precision: 0, unit: "VND", delimiter: ",", format: "%n %u")}", new_food_item_order_path(food_item_id: @food_item.id), class: 'btn btn-warning' %>
      </div>
    </div>
  </div>
  <div class="col-sm-4">
    <div class="card">
      <%= render 'reviews/form' %>
    </div><br>
    <div class="card">
      <h3 class="card-header" id="review">Review</h3>
      <%= render @food_item.reviews %>
    </div>
  </div>
</div>
</body>
</html>
